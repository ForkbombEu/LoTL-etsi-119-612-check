# TS602-04 pinned schema bundle and offline resolvers

## Task

Continue the `TODO.md` roadmap with TS602-04: pin the official ETSI TS
119 602 V1.1.1 JSON/XSD schemas and dependencies with reproducible
provenance, licenses, integrity metadata, and offline-only resolution.

## Commit

Pending at handoff creation; this note is committed with the implementation.

## Files changed

- `.gitattributes` — preserve exact upstream bytes for pinned vendor assets
- `schemas/etsi-ts-119-602/v1.1.1/` — exact ETSI Forge schema/license files,
  pinned W3C XML dependencies/license, manifest, and XML catalog
- `src/standards/ts119602Schemas.ts`
- `src/xml/xsd.ts`
- `test/ts119602Schemas.test.ts`, `test/xsd.test.ts`
- `README.md`, `TODO.md`
- This handoff note

## Commands/tests run

- `git ls-remote` against the official ETSI Forge repository to resolve tag
  `v1.1.1` to commit `e84f427f0cde99513b574ef4b5a155ac4a38eab6`
- SHA-256 and byte-length comparison against the downloaded official archive
- `pnpm exec vitest run test/ts119602Schemas.test.ts test/xsd.test.ts` — 2
  test files, 12 tests passed
- `pnpm test` — 22 test files, 98 tests passed
- `pnpm run build`
- `git diff --check`

## Generated Result Paths

- `schemas/etsi-ts-119-602/v1.1.1/` — committed runtime schema bundle,
  integrity manifest, licenses, dependencies, and offline catalog; these are
  intentional source assets rather than generated audit results.

## Generated artifacts intentionally not committed

- None. Temporary upstream downloads and extraction used only for byte/hash
  comparison were kept outside the repository under `/tmp`.

## Known caveats

- JSON Schema validation is not wired into assessment yet; that is TS602-05.
- The scheme-explicit XML assessment route does not yet invoke XSD validation
  with the pinned catalog. Generic `xmllint` validation now uses `--nonet`.
- The published TIE JSON schema refers to `1960201-jsonSchema.json`, while the
  published base filename uses underscores. The manifest records an explicit
  offline alias without modifying upstream bytes.
- W3C stable resource URLs do not expose repository commit identifiers; the
  manifest records retrieval date, exact URL, byte length, and SHA-256.

## Follow-up backlog

- TS602-05: validate the official JSON object/array binding with Draft-07 and
  isolate the legacy WE BUILD/TSL-like shape behind a compatibility adapter.
- Integrate scheme-explicit XML validation with the pinned XSD and catalog in
  the later XML binding-validation task.

## Surface changes

- CLI: changed (`xmllint` schema validation is now network-prohibited)
- API: unchanged
- OpenAPI: unchanged
- Validators: changed (bundle integrity/resolvers added; generic XSD hardened)
- Schemas: changed (official validation schema assets pinned; report schemas unchanged)
- Reports: unchanged
- Fixtures: unchanged
- Docs: changed
- Handoff policy: followed; policy itself unchanged
