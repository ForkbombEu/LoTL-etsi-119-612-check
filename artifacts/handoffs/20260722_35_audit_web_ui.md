# Audit web interface

- Task: browser frontend for LoTL and single-artifact audits.
- Commit: `4ab1681` (`feat: add browser audit interface`).
- Files changed: `src/api/auditUi.ts`, `src/api/routes.ts`,
  `test/api.test.ts`, `openapi/we-build-tl-audit.openapi.yaml`, `README.md`,
  and `TODO.md`.
- Commands/tests run: `npm run build`, `npm test`, and `git diff --check`.
- Validators/schemas/reports: no assessment semantics changed; the UI calls
  existing API routes and presents their report data.
- API/OpenAPI: added the browser interface route to OpenAPI; no new assessment
  operation was introduced.
- CLI: unchanged.
- Fixtures/docs: API route coverage added and README usage documented.
- Handoff policy: this note is intentionally uncommitted under ignored
  `artifacts/handoffs/`.

## Generated Result Paths

- `artifacts/handoffs/20260722_35_audit_web_ui.md` — intentionally uncommitted
  task handoff note; no generated audit report was produced.

## Known caveats

- The UI does not make a conformance or trust decision beyond the existing
  assessor output.
- URL audits can initiate network access only after the user explicitly submits
  the form; standard automated tests remain offline.
- Contextual evidence, signer evidence, RPAC chains and schema overrides remain
  available through the CLI/API rather than this initial UI.

## Follow-up backlog

- Add optional browser-level interaction tests if a browser test runner is
  adopted.
- Expose contextual evidence and certificate-chain inputs in a dedicated
  advanced workflow when a user-approved design is available.
